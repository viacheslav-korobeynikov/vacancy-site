package components

templ Title(title string) {
	@TitleStyle()
	<h1 class="title">{ title }</h1>
}

templ TitleStyle() {
	<style>
	.title{
		font-weight: bold;
		margin-bottom: 12px;
		margin-top: 0;
		color: var(--color-white);
		font-size: 70px;
	}
	</style>
}
